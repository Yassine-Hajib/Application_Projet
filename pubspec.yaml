name: caf
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Packages
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.0  # Added for Maps
  http: ^1.6.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # ------------------------------------------------
  # ASSETS SECTION (Indentation is very important!)
  # ------------------------------------------------
  assets:
    - assets/                 # Folder for avatar.png
    - lib/Frontend/Images/    # Folder for stadium images